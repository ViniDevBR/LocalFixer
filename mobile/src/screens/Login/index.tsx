import { View } from 'react-native'
import { ScreenProps } from '../../types/navigation'





export function Login({ route, navigation: { navigate }}: ScreenProps<'Login'>) {
  
  return (
    <View />
  )
}